You are working in the repository:
loofiboss-bit/plasma-ai-usage-monitor

Your task is to implement a direct product upgrade to v6.0.0, not just a review or plan.

This is a Plasma 6 / Qt6 / KF6 widget with:
- QML UI entrypoint in package/contents/ui/main.qml
- full popup UI in package/contents/ui/FullRepresentation.qml
- compact panel UI in package/contents/ui/CompactRepresentation.qml
- settings categories in package/contents/config/config.qml
- provider setup in package/contents/ui/configProviders.qml
- subscription + browser sync setup in package/contents/ui/configSubscriptions.qml
- C++ plugin/provider/tool backends in plugin/
- tests in plugin/tests/
- AppStream metadata in com.github.loofi.aiusagemonitor.metainfo.xml
- build metadata in CMakeLists.txt and package/metadata.json

Important context:
- The app already supports many providers and subscription tools.
- The next step is NOT adding many more integrations.
- The next step IS making the app feel like a polished, trustworthy, operator-grade product.
- Browser Sync already exists but should be treated as fragile/experimental.
- The settings surface is powerful but too heavy for first-run usability.
- The dashboard should become more action-oriented, not just information-dense.
- The product should clearly communicate whether data is actual, estimated, local, or limit-only.

Primary objective:
Ship a coherent v6.0.0 upgrade that improves usability, trust, clarity, diagnostics, and product maturity while preserving the existing architecture and provider support.

Execution rules:
- Do not only write plans. Implement the changes.
- Do not stop at TODO lists unless something is genuinely blocked.
- If a feature is too large, implement the safest minimal complete version now and leave a clearly documented follow-up note.
- Prefer improving the existing core experience over adding new providers.
- Keep Plasma 6 / Qt6 / KF6 compatibility intact.
- Preserve current functionality unless replacing it with something clearly better.
- Keep secrets handling secure and continue using KWallet patterns where appropriate.
- Keep Browser Sync behind an explicitly experimental/labs framing.
- Update tests, docs, screenshots references, version strings, metadata, and changelog as part of the release.
- Bump all project version references consistently to 6.0.0.

Deliverables:
1. Code changes
2. Updated tests
3. Updated README / docs / release notes / metadata
4. A final summary of what changed, what was intentionally deferred, and how to test it

Implement the following v6.0.0 scope:

==================================================
1. FIRST-RUN EXPERIENCE + GUIDED SETUP
==================================================

Build a proper guided first-run setup flow for the most important paths:
- OpenAI
- Anthropic
- Google Gemini
- Bedrock
- OpenRouter
- Loofi Server
- Claude Code
- Codex CLI
- GitHub Copilot

Requirements:
- Add a guided setup experience that is clearer than sending users straight into the full settings UI.
- Support a “recommended setup” path for common users.
- Add per-provider validation / “Test connection” style feedback before the user leaves setup.
- Show success/failure reasons in plain language:
  - key missing
  - invalid URL
  - endpoint unreachable
  - auth failure
  - feature available but data is estimated only
  - tool not installed
- Reuse existing backend knowledge where possible rather than inventing dummy validation.

Implementation guidance:
- You may extend the existing onboarding flow in FullRepresentation.qml or create a dedicated setup component if cleaner.
- Do not make setup depend on every provider. Focus on the most important/common ones.
- The experience should reduce cognitive load, not add another large configuration maze.

==================================================
2. BASIC / ADVANCED SETTINGS MODE
==================================================

Introduce progressive disclosure in settings.

Requirements:
- Add a Basic mode and an Advanced mode for provider/subscription configuration.
- Basic mode should show only the fields most users need:
  - enable switch
  - key/token
  - model
  - essential endpoint if needed
  - test/validation state
  - budget basics
- Advanced mode should reveal:
  - custom base URLs
  - tiers
  - org/PAT extras
  - experimental sync details
  - less common override fields
- The default experience should favor Basic mode.

Implementation guidance:
- Avoid duplicating the entire settings system if a clean conditional reveal is sufficient.
- Keep the UI maintainable; split large QML sections into reusable components if needed.
- Reduce the current visual overload in configProviders.qml and configSubscriptions.qml.

==================================================
3. DATA TRUST / CONFIDENCE LABELING
==================================================

Every important provider/tool metric should communicate what kind of data it is.

Add visible status labeling such as:
- Actual
- Estimated
- Local
- Rate-limit only
- Subscription-plan derived

Requirements:
- Surface this in cards and/or detail views, not just docs.
- Add concise tooltip/help text explaining the meaning.
- Keep wording consistent throughout the app.
- Make this visible enough that users do not mistake estimates for exact billing data.

Also add a simple provider confidence / health signal:
- healthy
- warning
- degraded
- disconnected

Base it on current backend state where possible.

==================================================
4. DASHBOARD REWORK FOR ACTIONABILITY
==================================================

Improve the live popup/dashboard so it answers:
- What is wrong right now?
- What needs action?
- What is most risky today?
- What changed recently?

Requirements:
- Add a stronger top summary area with items like:
  - top risk
  - budget left today
  - next reset
  - most critical provider/tool
- Make “Needs Attention” more actionable:
  - clearer reason text
  - better prioritization
  - quick action buttons where possible
- Improve provider/tool card behavior so the most relevant information is easier to scan.
- Preserve the current broad functionality, but improve hierarchy and readability.

Implementation guidance:
- Keep the popup performant.
- Avoid turning the UI into a wall of dense text.
- Use compact, operator-friendly language.

==================================================
5. COMPACT PANEL EXPERIENCE UPGRADE
==================================================

Make CompactRepresentation genuinely useful in daily use.

Requirements:
- Support pinning a preferred KPI mode:
  - daily cost
  - remaining requests
  - most critical provider
  - Loofi server KPI
  - connected count
- Improve accessibility and readability in compact mode.
- Add better quick actions through contextual actions/menu if reasonable:
  - refresh all
  - open settings
  - open history
  - mute/snooze alerts temporarily
- Ensure the compact view remains understandable at typical panel sizes.

Implementation guidance:
- Reuse existing display mode concepts if possible.
- Do not remove current compact functionality unless replaced cleanly.

==================================================
6. DIAGNOSTICS CENTER
==================================================

Create a clearer diagnostics surface for users.

Requirements:
- Add a diagnostics view/section that consolidates:
  - provider validation state
  - browser sync readiness
  - wallet/secrets readiness
  - local dependency readiness if detectable
  - history DB readiness
  - plugin/runtime readiness where practical
- Reuse current install_doctor.sh logic conceptually where possible.
- Show user-friendly explanations and suggested fixes.

This does not need to replicate the shell script fully inside the widget, but it should expose the most useful runtime diagnostics from inside the app.

==================================================
7. BROWSER SYNC -> LABS / EXPERIMENTAL
==================================================

Refactor Browser Sync positioning so it is explicitly experimental.

Requirements:
- Move it into a Labs / Experimental framing in the settings/UI.
- Add stronger warnings that it depends on undocumented/internal service behavior.
- Improve status breakdown:
  - browser profile found
  - cookie DB found
  - cookies found
  - session valid
  - connection possible
- Keep current functionality where possible.
- Avoid making Browser Sync feel like a guaranteed core feature.

Implementation guidance:
- Do not remove Browser Sync.
- Make it easier to understand why it fails.
- Make the support boundary clearer.

==================================================
8. PRESETS / PROFILES
==================================================

Add user-facing presets/profiles for the app configuration.

Suggested presets:
- Solo Developer
- Multi-Provider
- Local-First
- Budget Watch
- Loofi Operator

Requirements:
- A preset should configure sensible defaults for UI behavior, alerting emphasis, and visible priorities.
- Do not overwrite secrets.
- Keep preset application reversible where possible.
- The goal is to help users start from a sane configuration quickly.

==================================================
9. IMPORT / EXPORT (SAFE CONFIG ONLY)
==================================================

Add safe configuration portability.

Requirements:
- Add export/import for non-secret configuration.
- Never export API keys, tokens, session secrets, or wallet data.
- Provide a clear warning that secrets are excluded.
- Choose a simple stable format such as JSON.
- Include versioning in exported config.

==================================================
10. PERFORMANCE + MAINTAINABILITY PASS
==================================================

Do a focused cleanup pass while implementing v6.0.0.

Requirements:
- Reduce UI heaviness where possible.
- Prefer lazy loading for expensive or rarely used views.
- Split oversized QML files if that meaningfully improves maintainability.
- Avoid unnecessary refresh work.
- Keep the codebase consistent and easier to extend.

Also:
- If the current settings or popup files are too large, refactor into reusable components.
- Preserve behavior and readability.

==================================================
11. TESTS + RELEASE QUALITY
==================================================

Update the test suite and release assets.

Requirements:
- Add or update tests for new logic where practical.
- Ensure existing tests still pass.
- Add focused regression protection for:
  - settings mode behavior
  - provider data classification/confidence logic
  - preset application
  - safe config import/export
- Update:
  - README
  - CHANGELOG
  - AppStream metadata
  - version strings in all relevant files
  - screenshots references if needed
  - any release notes or user guide areas impacted

Versioning:
- Bump project version to 6.0.0 consistently everywhere.
- Remove or fix any version drift in metadata/docs.

==================================================
12. OUTPUT FORMAT YOU MUST FOLLOW
==================================================

At the end, provide:
1. A concise release summary for v6.0.0
2. A bullet list of changed files grouped by area
3. A testing checklist
4. A short list of intentionally deferred items, only if truly necessary

Working style:
- Inspect the repository first and make grounded changes.
- Prefer implementation over commentary.
- Be decisive.
- Ship a cohesive v6.0.0, not a partial brainstorm.
